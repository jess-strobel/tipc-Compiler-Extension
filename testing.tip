main(n) {
    var j;
    var y;
    var z;
    j = - -3;
    y = false;
    z = (j == 0) ? 1 : 2; 
    if (n > 4 and not (j == 0)) {
        n = 1;
        n++;
        n--;
        n = n % 2;
    }

    for (j : j < 5) {
        n++;
    }

    for (j : 5 .. 9) {
        n++;
    }
    return 0;
}
