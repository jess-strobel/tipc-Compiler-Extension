main(n) {
    var a;
    var b;
    var j;
    var x;
    var y;
    var z;
    a = true and (1 > 0);
    j = --3;
    z = (j == 0) ? 1 : 2; 
    x = [1, 2, 3];
    x = [3 of 7];
    y = #[1, 2, 3];
    y = #[3 of 7];
    if (n > 4 and not (j == 0)) {
        n = 1;
        n++;
        n--;
        n = n % 2;
    }

    for (j : x) {
        n++;
    }

    for (j : 5 .. 9) {
        n++;
    }
    return 0;
}
